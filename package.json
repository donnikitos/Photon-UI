{
	"name": "Photon-UI",
	"version": "1.0.0",
	"description": "Principles applied by the Mozilla Photon guide, to design and build the next generation of Firefox-related and other products.",
	"scripts": {
		"build": "npx sass ./src/main.scss:build/photon-ui.css",
		"build-watch": "npx sass ./src/main.scss:build/photon-ui.css --watch",
		"build-min": "npx sass ./src/main.scss:build/photon-ui.min.css --style compressed"
	},
	"author": "Nikita Nitichevski <me@donnikitos.com> (https://donnikitos.com)",
	"license": "MIT",
	"devDependencies": {
		"sass": "^1.27.0"
	}
}
